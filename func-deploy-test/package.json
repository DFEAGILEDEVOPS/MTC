{
  "name": "function-test-app",
  "version": "0.1.0",
  "scripts": {
    "build:production": "echo TODO",
    "start:dev": "cd ../tslib && yarn build && cd ../func-deploy-test && func extensions install && yarn start:host",
    "start:host": "func start"
  },
  "description": "",
  "devDependencies": {
    "@azure/functions": "^1.0.1-beta1",
    "gulp": "^4.0.2",
    "gulp-replace": "^1.0.0",
    "gulp-typescript": "^5.0.1",
    "typescript": "^3.3.3"
  },
  "dependencies": {
    "dotenv": "^8.1.0"
  }
}
