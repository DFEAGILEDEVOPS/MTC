version: '3'
services:
  pupil-spa:
    build: ./pupil-spa
    container_name: mtc_pupil_spa
    ports:
    - "3002:80"
  admin-app:
    build: ./admin
    container_name: mtc_admin
    ports:
    - "3001:3001"
#    volumes:
#    - ./admin:/usr/src/app
    environment:
      MONGO_CONNECTION_STRING: "mongodb://mtc_db/mtc"
    depends_on:
#    - esb
    - mongo
    links:
#    - esb
    - mongo
#  esb:
#    image: rabbitmq
#    container_name: mtc_esb
  mongo:
    image: mongo
    container_name: mtc_db
    ports:
    - "27017:27017"