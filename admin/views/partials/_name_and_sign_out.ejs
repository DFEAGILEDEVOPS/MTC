<% if (isAuthenticated) { %>
<div id="sign-out">
    <a href="/sign-out">Sign out</a>
</div>
<% if (typeof userName != 'undefined') { %>
<div class="signed-in-as">Signed in as <%= userName %></div>
<% } %>
<% } %>
