<div class="grid-row">
    <div class="column-one-half">
        <%- partial('partials/_breadcrumbs', { breadcrumbs: breadcrumbs }) %>
    </div>
    <div class="column-one-half">
        <%- partial('partials/_name_and_sign_out') %>
    </div>
</div>

<div class="grid-row">
    <div class="column-two-thirds">
        <h1 class="heading-xlarge">Form <%= form.name %></h1>

        <div class="panel panel-border-wide">
            <% if (form.checkWindows.length > 0) { %>
            This form is assigned to: <%= form.checkWindows.map( cw => { return cw.name; }).join(', '); %>
            <% } else { %>
            This form is unassigned
            <% } %>
        </div>

        <div class="padding-top">
            <dl class="inline-definition-list" tabindex="0" role="list">
                <% form.questions.forEach((q) => { %>
                <dt class="inline-definition-list" tabindex="0" role="listitem">Q<%= num++ %></dt>
                <dd class="inline-definition-list">
                    <span class="fixed-factors" tabindex="0" role="listitem"><%= q.f1 %> &times; <%= q.f2 %></span>
                    <span class="equals-sign" tabindex="0" role="listitem">=</span>
                    <span class="fixed-result" tabindex="0" role="listitem"><%= (q.f1 * q.f2) %></span>
                </dd>
                <% }); %>
            </dl>
            <br />
            <% if (canDelete) { %>
            <a href="/test-developer/delete-form/<%= form._id %>" class="modal-link" id="js-modal-link">Remove form</a>
            <% } %>
        </div>
        <div class="back-to-top-container">
            <a href="#top">Back to top</a>
        </div>
    </div>
    <div class="column-one-third add-title-margin">
        <aside role="complementary" class="support-column">
            <h3 class="heading-medium">Related</h3>
            <nav role="navigation">
                <ul class="list">
                    <li>
                        <a href="/PDFs/MTC_administration_guidance_June-2017-trial.pdf" target="_blank">Guidance</a>
                    </li>
                    <li>
                        <a href="#">Assign forms to check window</a>
                    </li>
                </ul>
            </nav>
        </aside>
    </div>
</div>
<%- partial('partials/_modal_box', {
    modalTitle: 'Please confirm',
    modalText: 'Are you sure you want to remove this?',
    modalConfirmationLabel: 'Yes',
    modalConfirmationUrl: '#',
    modalCancelLabel: 'No',
    modalCancelUrl: '#'
}) %>
