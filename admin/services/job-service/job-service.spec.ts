
describe('Job Service', () => {
  afterEach(() => {
    jest.restoreAllMocks()
  })

  describe('getJobSummary', () => {
    test.todo('returns array of jobs in expected format')
  })

  describe('getJobOutputs', () => {
    test.todo('throws an error if jobId not provided')
    test.todo('returns job outputs as zip file')
  })
})
