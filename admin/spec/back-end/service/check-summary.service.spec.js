'use strict'

/* global describe it expect */

const sut = require('../../../services/check-summary.service')

describe('check-summary service', () => {
  it('should be defined', () => {
    expect(sut).toBeDefined()
  })
})
