'use strict'

module.exports = {
  questionTimeLimit: 'Question time limit requires a number between 1 and 60, decimals are valid',
  loadingTimeLimit: 'Time between questions requires a number between 1 and 5, decimals are valid'
}
