-- Originally this PR was going to re-create the Azure reccommended index on pupil
-- following code review comments this is now now longer required, so this note exists
-- instead of the drop statement as the index has already been dropped.

