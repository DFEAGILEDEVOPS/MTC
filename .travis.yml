language: node_js

env:
  - CXX=g++-4.8

node_js: 
  - 8.1.0

addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    packages:
    - g++-4.8

sudo: false

before_install:
  - $CXX --version

install: true

script: sh -c 'cd pupil && npm install  && cd ../admin && npm install'

