# currently unused. retained for potential future use to validate docker image builds
sudo: required
services:
  - docker
node_js:
  - 10.16.3
before_install:
  - node ./deploy/should-it-build.js $TRAVIS_PULL_REQUEST
script:
  - cd admin
  - docker build --tag $TRAVIS_PULL_REQUEST .
after_script:
  - docker images
